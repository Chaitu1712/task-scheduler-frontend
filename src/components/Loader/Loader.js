import React from 'react';
import './Loader.css'; // Add some styling

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;